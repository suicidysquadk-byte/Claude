/* Configuração do servidor do BattleHub.
   Preencha com os dados do seu projeto no Supabase:
   Painel do Supabase → Project Settings → API.
   A chave "anon public" pode ficar no app: quem protege os dados são as regras do banco. */
window.BH_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  // endereço de retorno do login com Google no app Android (igual ao appId do capacitor.config.json)
  appScheme: 'gg.battlehub.app'
};
